version: '3.8'

# Tenant-specific configuration for Validation Corp
# Apply with: docker compose -f docker-compose.yml -f tenant_05f625700803348d/docker-compose.override.yml up

services:
  command-center:
    environment:
      - TENANT_ID=tenant_05f625700803348d
      - TENANT_NAME=Validation Corp
      - TENANT_DOMAIN=validation.com
      - API_KEY=TNPvq7yDDtceVHgdDOZvb7KZt3Db7TnK
      - ADMIN_KEY=33p6iP2GUxqom7yDOS5KpcvyVifE4gYR
      - PLAN_ID=starter
      - MAX_OPERATIONS_PER_MONTH=10000
      - MAX_AI_ACTIONS_PER_MONTH=1000
      - MAX_ALERTS_PER_DAY=1000
      - MAX_API_CALLS_PER_MINUTE=60
      - MAX_STORAGE_GB=10
      - MAX_USERS=5
    volumes:
      - ./data/tenant_05f625700803348d:/app/data
      - ./audit/tenant_05f625700803348d:/app/audit
