version: '3.8'

# Tenant-specific configuration for Enterprise Validation Inc
# Apply with: docker compose -f docker-compose.yml -f tenant_2438dcfe53fa52ca/docker-compose.override.yml up

services:
  command-center:
    environment:
      - TENANT_ID=tenant_2438dcfe53fa52ca
      - TENANT_NAME=Enterprise Validation Inc
      - TENANT_DOMAIN=enterprise-validation.com
      - API_KEY=HhaLBDxB5NnEm1X0YNfUuXRzb6LoJonD
      - ADMIN_KEY=TakbwA4GTddoPAh89rNIrALRqW2vpcqr
      - PLAN_ID=enterprise
      - MAX_OPERATIONS_PER_MONTH=1000000
      - MAX_AI_ACTIONS_PER_MONTH=100000
      - MAX_ALERTS_PER_DAY=50000
      - MAX_API_CALLS_PER_MINUTE=1000
      - MAX_STORAGE_GB=1000
      - MAX_USERS=100
    volumes:
      - ./data/tenant_2438dcfe53fa52ca:/app/data
      - ./audit/tenant_2438dcfe53fa52ca:/app/audit
