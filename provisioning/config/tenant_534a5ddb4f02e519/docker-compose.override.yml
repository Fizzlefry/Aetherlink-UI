version: '3.8'

# Tenant-specific configuration for Validation Corp
# Apply with: docker compose -f docker-compose.yml -f tenant_534a5ddb4f02e519/docker-compose.override.yml up

services:
  command-center:
    environment:
      - TENANT_ID=tenant_534a5ddb4f02e519
      - TENANT_NAME=Validation Corp
      - TENANT_DOMAIN=validation.com
      - API_KEY=p5BF5GMegbg2w6Kt4bKB322VK20bjrSV
      - ADMIN_KEY=TJhqzOLCimPGEPuHKp8tyuP6r6AjWRq4
      - PLAN_ID=starter
      - MAX_OPERATIONS_PER_MONTH=10000
      - MAX_AI_ACTIONS_PER_MONTH=1000
      - MAX_ALERTS_PER_DAY=1000
      - MAX_API_CALLS_PER_MINUTE=60
      - MAX_STORAGE_GB=10
      - MAX_USERS=5
    volumes:
      - ./data/tenant_534a5ddb4f02e519:/app/data
      - ./audit/tenant_534a5ddb4f02e519:/app/audit
