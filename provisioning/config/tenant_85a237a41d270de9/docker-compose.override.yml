version: '3.8'

# Tenant-specific configuration for Enterprise Validation Inc
# Apply with: docker compose -f docker-compose.yml -f tenant_85a237a41d270de9/docker-compose.override.yml up

services:
  command-center:
    environment:
      - TENANT_ID=tenant_85a237a41d270de9
      - TENANT_NAME=Enterprise Validation Inc
      - TENANT_DOMAIN=enterprise-validation.com
      - API_KEY=7uRSG4GO6VknugdVAjQRLkI17tUiU1B6
      - ADMIN_KEY=I8Xc3gacEnwDraK6qTeJ6d6yrF5HYpl8
      - PLAN_ID=enterprise
      - MAX_OPERATIONS_PER_MONTH=1000000
      - MAX_AI_ACTIONS_PER_MONTH=100000
      - MAX_ALERTS_PER_DAY=50000
      - MAX_API_CALLS_PER_MINUTE=1000
      - MAX_STORAGE_GB=1000
      - MAX_USERS=100
    volumes:
      - ./data/tenant_85a237a41d270de9:/app/data
      - ./audit/tenant_85a237a41d270de9:/app/audit
