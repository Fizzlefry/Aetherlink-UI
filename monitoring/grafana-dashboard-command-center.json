{
  "id": null,
  "uid": "aetherlink-cc-ops",
  "title": "AetherLink Command Center – Ops",
  "tags": ["aetherlink", "command-center", "ops"],
  "timezone": "browser",
  "schemaVersion": 38,
  "version": 1,
  "refresh": "10s",
  "style": "dark",
  "panels": [
    {
      "type": "stat",
      "title": "Scheduler Imports by Tenant",
      "id": 1,
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 0 },
      "options": {
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "orientation": "auto",
        "colorMode": "value",
        "graphMode": "none",
        "justifyMode": "auto"
      },
      "targets": [
        {
          "refId": "A",
          "expr": "sum by (tenant) (aetherlink_acculynx_scheduled_imports_total)",
          "legendFormat": "{{tenant}}",
          "datasource": { "type": "prometheus", "uid": "Prometheus" }
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Scheduler Import Rate (5m)",
      "id": 2,
      "gridPos": { "h": 6, "w": 12, "x": 6, "y": 0 },
      "options": {
        "legend": { "showLegend": true },
        "tooltip": { "mode": "single" }
      },
      "fieldConfig": {
        "defaults": { "unit": "ops" },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "expr": "sum by (tenant) (rate(aetherlink_acculynx_scheduled_imports_total[5m]))",
          "legendFormat": "{{tenant}}",
          "datasource": { "type": "prometheus", "uid": "Prometheus" }
        }
      ]
    },
    {
      "type": "table",
      "title": "Local Actions by Tenant / Action",
      "id": 3,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 6 },
      "options": {
        "showHeader": true
      },
      "fieldConfig": {
        "defaults": { "custom": {} },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "expr": "aetherlink_local_actions_total",
          "legendFormat": "{{tenant}} {{action}}",
          "datasource": { "type": "prometheus", "uid": "Prometheus" }
        }
      ],
      "transformations": [
        {
          "id": "organize",
          "options": {
            "indexByName": {
              "Time": 0,
              "Value": 1,
              "tenant": 2,
              "action": 3
            },
            "renameByName": {
              "Value": "count"
            }
          }
        }
      ]
    },
    {
      "type": "table",
      "title": "Raw Scheduler Metrics",
      "id": 4,
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 6 },
      "options": { "showHeader": true },
      "targets": [
        {
          "refId": "A",
          "expr": "aetherlink_acculynx_scheduled_imports_total",
          "legendFormat": "{{tenant}}",
          "datasource": { "type": "prometheus", "uid": "Prometheus" }
        }
      ]
    }
  ],
  "time": {
    "from": "now-6h",
    "to": "now"
  },
  "timepicker": {
    "refresh_intervals": ["5s", "10s", "30s", "1m", "5m", "15m"]
  }
}
