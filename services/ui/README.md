# AetherLink UI — Components

This UI surfaces live operational status and drill-downs for Command Center and Operator workflows.

## Operator Dashboard Components

- MediaStatsCard — Shows current upload metrics; supports optional `jobId` prop to deep-link to that job’s media.
- MediaStatsBadge — Compact header badge showing uploads_today and total_size_mb.
